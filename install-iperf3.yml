- name: Set debconf selection for iperf3 autostart
  shell: echo "iperf3 iperf3/autostart boolean true" | debconf-set-selections

- name: Install iperf3
  apt:
    name: iperf3
    state: present
    update_cache: yes
